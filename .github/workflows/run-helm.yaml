---
    on:
      push:
        branches: [main, master, develop]

    jobs:
      build:
        runs-on: ubuntu-latest
        steps:
          - name: Checkout code
            uses: actions/checkout@v4

          - run: ls -la
          - run: pwd
          - run: helm version
          - run: helm template --values=values.yaml --version=2.13.1 oci://harbor.3key.company/czertainly-helm/czertainly